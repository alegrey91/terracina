---
page_title: terracina workspace show command reference
description: The terracina workspace show command outputs the current workspace.
---

# `terracina workspace show` command

The `terracina workspace show` command outputs the current workspace.

## Usage

Usage: `terracina workspace show`

The command displays the current workspace.

## Example

```
$ terracina workspace show
development
```
