---
page_title: terracina workspace select` command reference
description: The terracina workspace select command selects a workspace.
---

# `terracina workspace select` command

The `terracina workspace select` selects a different workspace to use for further operations.

## Usage

Usage: `terracina workspace select NAME [DIR]`

This command will select another workspace. The named workspace must already
exist.

The supported flags are:

* `-or-create` - If the workspace that is being selected does not exist, create it. Default is `false`.

## Example

```
$ terracina workspace list
  default
* development
  jsmith-test

$ terracina workspace select default
Switched to workspace "default".
```
